export type FieldType = {
  username?: string;
  password?: string;
  remember?: string;
};

export type ValuesType = {
  username: string;
  password: string;
};
export interface DataType {
  key: React.Key;
  name: string;
  age: number;
  address: string;
}
